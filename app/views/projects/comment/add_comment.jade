form.form-vertical(method="post", action="/projects/#{project._id}/create_comment")
  input(type="hidden", name='_csrf', value=token)
  .control-group
    .controls
      textarea#comment.input-xxlarge(type='text', rows="6", name="comment", placeholder='写上我的回复')
  button.btn.btn-primary(type='submit') 回复
